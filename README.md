# dynamic-ip-loadbalancer-controller
this is a lightweigt kubernetes component for keeping the external-ip of your loadbalancer services uptodate when running a kubernetes cluster at home behind a router with a dynamic ip
